
# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /                           controllers.PortofolioController.portofolioPage()
GET     /message-board              controllers.MessageBoardController.messageBoardPage()
GET     /message-board/add          controllers.MessageBoardController.messageBoardAddNewMessage()
GET     /message-board/search          controllers.MessageBoardController.messageBoardSearchMessage()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

      